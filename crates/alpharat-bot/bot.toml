[settings]
name = "AlphaRat"
agent_id = "alpharat/mcts"
run_command = "cargo run --release --features onnx-coreml"

[details]
description = "MCTS with decoupled PUCT search, configurable simulation budget and exploration"
developer = "mintiti"
language = "Rust"
tags = ["mcts", "tree-search", "alphazero", "reinforcement-learning"]
