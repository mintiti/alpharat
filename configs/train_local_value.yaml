# LocalValueMLP training configuration
# Usage: uv run python scripts/train_local_value.py configs/train_local_value.yaml

model:
  hidden_dim: 256
  dropout: 0.0

optim:
  lr: 1e-3
  policy_weight: 1.0
  value_weight: 1.0
  ownership_weight: 1.0
  loss_variant: dqn
  p_augment: 0.5

data:
  train_dir: data/940b6e74-01c1-4a2e-ac6d-9ca2c1ec54c5/train
  val_dir: data/940b6e74-01c1-4a2e-ac6d-9ca2c1ec54c5/val

seed: 42
