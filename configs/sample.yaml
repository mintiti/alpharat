# Self-play sampling configuration
# Usage: alpharat-sample configs/sample.yaml

group: uniform_5x5  # Required: batch group name

mcts:
  variant: decoupled_puct
  simulations: 554
  c_puct: 8.342921588471766
  force_k: 0.8834383744890093

game:
  width: 5
  height: 5
  max_turns: 30
  cheese_count: 5
  wall_density: 0.0
  mud_density: 0.0

sampling:
  num_games: 50000
  workers: 4

# experiments_dir: experiments  # Default, override if needed
checkpoint: null               # Path to model checkpoint, or null for uniform prior
