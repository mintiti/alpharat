# Self-play sampling configuration
# Usage: uv run python scripts/sample.py configs/sample.yaml

mcts:
  variant: decoupled_puct
  simulations: 200
  c_puct: 4.73057493635652

game:
  width: 5
  height: 5
  max_turns: 30
  cheese_count: 5
  wall_density: 0.0
  mud_density: 0.0

sampling:
  num_games: 50000
  workers: 4

output_dir: batches/
checkpoint: null             # Path to model checkpoint, or null for uniform prior
