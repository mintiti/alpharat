# Phase 1 Experiment: MCTS vs Random
# Goal: Sanity check that MCTS beats random on 5x5 with 99% confidence
# Usage: uv run python scripts/benchmark.py configs/mcts_vs_random.yaml

name: mcts_vs_random  # Required: benchmark name

agents:
  puct_200:
    variant: decoupled_puct
    simulations: 200
    c_puct: 4.73057493635652

games_per_matchup: 200

game:
  width: 5
  height: 5
  max_turns: 30
  cheese_count: 5
  wall_density: 0.0
  mud_density: 0.0

workers: 4
