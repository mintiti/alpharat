# @package _global_

# Self-play sampling: 7x7 open maze, asymmetric cheese
# Usage: alpharat-sample configs/sample/7x7_open_asymmetric.yaml

defaults:
  - /game: 7x7_open_asymmetric
  - /mcts: 5x5_tuned
  - _self_

group: 7x7_open_asymmetric

sampling:
  num_games: 50000
  workers: 4

checkpoint: null  # Path to model checkpoint, or null for uniform prior
