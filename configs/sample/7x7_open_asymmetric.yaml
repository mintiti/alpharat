# @package _global_

# Self-play sampling: 7x7 asymmetric starts
# Usage: alpharat-sample configs/sample/7x7_asymmetric.yaml

defaults:
  - /game: 7x7_asymmetric
  - /mcts: 5x5_tuned
  - _self_

group: asymmetric_7x7

sampling:
  num_games: 50000
  workers: 4

checkpoint: null  # Path to model checkpoint, or null for uniform prior
