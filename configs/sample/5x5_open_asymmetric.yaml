# @package _global_

# Self-play sampling: 5x5 asymmetric starts
# Usage: alpharat-sample configs/sample/5x5_asymmetric.yaml

defaults:
  - /game: 5x5_asymmetric
  - /mcts: 5x5_tuned
  - _self_

group: asymmetric_5x5

sampling:
  num_games: 50000
  workers: 4

checkpoint: null  # Path to model checkpoint, or null for uniform prior
