# Self-play sampling with NN prior
# Usage: uv run python scripts/sample.py configs/sample_with_nn.yaml --group nn_guided_v1

mcts:
  variant: decoupled_puct
  simulations: 554
  c_puct: 8.342921588471766
  force_k: 0.8834383744890093

game:
  width: 5
  height: 5
  max_turns: 30
  cheese_count: 5
  wall_density: 0.0
  mud_density: 0.0

sampling:
  num_games: 50000
  workers: 4
  device: auto  # Use "auto", "cpu", "cuda", or "mps"

# Note: output_dir is now controlled via --group argument
checkpoint: experiments/runs/train_20251227_231834/checkpoints/best_model.pt
